apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/jamesob131/reactjenkins:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/jamesob131/reactjenkins:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: reactjenkins
        chartPath: charts/reactjenkins
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/jamesob131/reactjenkins"
          image.tag: "{{.DIGEST_HEX}}"
